upstream app_upstream {
    server app:9000;
}

server {
    listen 80;
    server_name codeunion.local *.codeunion.local;
    root /app/public;
    index index.php;

    # Redirect to HTTPS in production
    # return 301 https://$server_name$request_uri;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }

    # Deny access to Laravel directories
    location ~ ^/(storage|bootstrap)/ {
        deny all;
    }

    # Static assets with cache headers
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header "Cache-Control" "public, immutable";
    }

    # Main application routing
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP handler
    location ~ \.php$ {
        # Security
        fastcgi_pass app_upstream;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        
        # Performance
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_connect_timeout 60;
        fastcgi_send_timeout 300;
        fastcgi_read_timeout 300;
    }
}

# HTTPS configuration (uncomment for production with valid SSL certificates)
# server {
#     listen 443 ssl http2;
#     server_name codeunion.local *.codeunion.local;
#     root /app/public;
#     index index.php;
#
#     ssl_certificate /etc/nginx/ssl/certs/codeunion.crt;
#     ssl_certificate_key /etc/nginx/ssl/private/codeunion.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Redirect HTTP to HTTPS
#     if ($scheme != "https") {
#         return 301 https://$server_name$request_uri;
#     }
# }
